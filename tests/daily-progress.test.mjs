import test from 'node:test';import assert from 'node:assert/strict';import {loadTypeScript} from './load-typescript.mjs';
const {dailyProgress,isTaskOnDay}=loadTypeScript('lib/daily-progress.ts');
test('four complete out of five scheduled activities is 80%, including unlogged activities',()=>{const day='2026-09-14';const habits=Array.from({length:5},(_,i)=>({scheduledDays:[1],logsByDate:i<4?{[day]:'complete'}:{}}));assert.equal(dailyProgress(habits,[],day,'UTC').completion,80);});
test('unscheduled gym is excluded, rest and vacation stay neutral, tasks count once',()=>{const day='2026-09-15';const habits=[{scheduledDays:[1,3,5],logsByDate:{}},{scheduledDays:[2],logsByDate:{[day]:'rest'}},{scheduledDays:[2],logsByDate:{[day]:'complete'}}];const task={status:'pending',due_at:'2026-09-16T02:00Z',scheduled_start:'2026-09-15T18:00Z'};const result=dailyProgress(habits,[task],day,'America/Toronto');assert.equal(result.total,3);assert.equal(result.completion,50);assert.equal(isTaskOnDay({...task,status:'cancelled'},day,'America/Toronto'),false);});
